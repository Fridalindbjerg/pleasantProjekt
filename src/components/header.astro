<header class="header">
  <div class="header-left">
    <button class="search-btn light">
      <span>Søg...</span>
    </button>
  </div>

  <div class="header-center">
    <div class="logo-wrapper">
      <img src="/logoimg.png" alt="Pleasant logo" class="header-logo" />
    </div>
    <nav class="nav-links">
      <a href="#">Kasketter</a>
      <a href="#">Tøj</a>
      <a href="#">Livsstil</a>
      <a href="#">Drops</a>
    </nav>
  </div>

  <div class="header-right">
    <button class="search-btn dark">
      <span>Søg...</span>
    </button>
  </div>

  <!-- Burger menu button -->
  <div class="burger-menu">
    <span class="burger-line"></span>
    <span class="burger-line"></span>
    <span class="burger-line"></span>
  </div>
</header>

<style>
  /* Header styling */
  .header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: #f8f5ed;
    padding: 1.5rem 2rem;
    font-family: sans-serif;
  }

  .header-left,
  .header-right {
    flex: 1;
    display: flex;
    justify-content: center;
  }

  .header-center {
    flex: 2;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .logo-wrapper {
    height: 60px;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .header-logo {
    height: 150px;
    object-fit: cover;
  }

  .nav-links {
    display: flex;
    gap: 2rem;
    font-weight: bold;
  }

  .nav-links a {
    color: #000;
    text-decoration: none;
    font-size: 0.95rem;
  }

  .search-btn {
    display: flex;
    align-items: center;
    font-weight: bold;
    font-size: 0.9rem;
    padding: 0.6rem 1.2rem;
    border-radius: 999px;
    border: 1px solid #000;
    background: transparent;
    cursor: pointer;
  }

  .search-btn.dark {
    background: #131313;
    color: #fff;
    border: none;
  }

  .search-btn.light {
    background: transparent;
    color: #000;
  }

  /* Burger Menu (for mobile only) */
  .burger-menu {
    display: none;
    flex-direction: column;
    gap: 5px;
    cursor: pointer;
  }

  .burger-line {
    width: 30px;
    height: 3px;
    background-color: #000;
  }

  @media (max-width: 768px) {
    .header {
      gap: 1rem;
    }

    .header-left,
    .header-right,
    .header-center,
    .nav-links {
      display: none;
    }

    .burger-menu {
      display: flex;
    }
  }
</style>
